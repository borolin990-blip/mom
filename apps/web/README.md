# @mom/web

The Next.js frontend + thin API layer.

**Placeholder in Phase 0.** The application shell, auth, onboarding, content
workspace, generation UI, calendar, and dashboard are built from **Phase 1.2**
onward — see [`../../docs/ROADMAP.md`](../../docs/ROADMAP.md).

This layer will consume `@mom/core` and `@mom/db` and must not contain business
logic, AI, or ORM code directly.
